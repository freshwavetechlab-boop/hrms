# WARNING: true means API startup will overwrite local payroll DB from the bootstrap SQL.
# Set false after first successful import if you want to keep later local changes.
HRMS_BOOTSTRAP_DATA=true
HRMS_BOOTSTRAP_ONCE=true
HRMS_BOOTSTRAP_SQL=Database/2026-06-28_local_bootstrap.sql
